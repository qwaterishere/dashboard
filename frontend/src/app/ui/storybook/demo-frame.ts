/** Общая рамка для playground stories atoms/molecules. */
export const ATOM_DEMO_PANEL =
  'display:block;width:min(360px,calc(100vw - 48px));padding:20px 24px;box-sizing:border-box;background:var(--card);border:1px solid var(--line);border-radius:12px;';

export const ATOM_DEMO_FIELD =
  'display:block;width:min(280px,calc(100vw - 48px));';

export const ATOM_PROGRESS_WIDTH =
  'display:block;width:min(280px,calc(100vw - 48px));';

export const ATOM_MARK_TRACK =
  'position:relative;width:min(280px,calc(100vw - 48px));border-radius:99px;background:var(--surface-inset);';

/** Широкая рамка для period-bar, panel-header и т.п. */
export const MOLECULE_DEMO_WIDE =
  'display:block;width:min(720px,calc(100vw - 48px));padding:20px 24px;box-sizing:border-box;background:var(--card);border:1px solid var(--line);border-radius:12px;';

/** Узкая колонка под sidebar nav-item. */
export const MOLECULE_SIDEBAR =
  'display:block;width:240px;padding:8px;box-sizing:border-box;background:var(--card);border:1px solid var(--line);border-radius:12px;';
