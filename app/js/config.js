// Базовый адрес API.
//
// Вариант A (рекомендуется): FastAPI отдаёт и статику, и API с одного адреса.
//   Тогда оставьте '/api' — запросы идут на тот же origin, CORS не нужен.
//
// Вариант B: фронтенд на одном порту (python -m http.server), бэкенд на другом.
//   Укажите полный адрес, например 'http://localhost:8000/api',
//   и включите CORS в backend/app.py (там это уже подготовлено).
//
// Если API недоступен, фронтенд автоматически берёт локальные data/<имя>.json
// (см. app/js/api.js) — поэтому страницы работают и без бэкенда.
export const API_BASE = '/api';
