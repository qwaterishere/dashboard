[pytest]
markers =
    onboarding: живой аудит iiko нового ресторана по стандарту внедрения (запуск: pytest -m onboarding)
; onboarding-тесты ходят в живое API — из обычного прогона исключены,
; явный -m onboarding в командной строке перекрывает этот фильтр
addopts = -m "not onboarding"
