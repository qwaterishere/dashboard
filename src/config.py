"""Глобальные настройки приложения.

Значения берутся из переменных окружения, чтобы не хардкодить их в коде
(например, путь к БД на проде). Локально работают значения по умолчанию.
"""
import os

DB_URL = os.getenv('DB_URL', 'sqlite:///dashboard.db')
